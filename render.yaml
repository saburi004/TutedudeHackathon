services:
  - type: web
    name: tutedude-nextjs-app
    env: node
    plan: free
    branch: master
    buildCommand: npm install && npm run build
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
